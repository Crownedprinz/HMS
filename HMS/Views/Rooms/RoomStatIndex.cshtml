@model  IEnumerable<HMS.Models.vw_genlay>
@using anchor1.Helpers
@{
    ViewBag.Title = "Rooms Status";
}


<div class="panel panel-default">
    <div class="panel-heading">
        Rooms Status
  @*      <span>@Html.EncodedActionLink("New Room", "Create", null, new { pc = 1 }, new { title = "New Record" }) </span>*@
    </div>
    <div class="panel-body">
        <div class="table-responsive">
            <table class="hTable table table-striped table-bordered table-hover">

                <thead>
                    <tr>
                        <th>Room Number</th>
                        <th>Room Type</th>
                        <th>Status</th>
                        <th>Change</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.vwstring0</td>
                            <td>@item.vwstring3</td>
                            <td>@item.vwstring2</td>
                            @if(@item.vwstring4 =="03" ){
                            <td></td>
                            }else{
                        <td>
                            @Html.EncodedActionLink("C", "RoomStat", null, new { Key1 = @item.vwstring0 }, new { @class = "btn btn-warning btn-xs glyphicon glyphicon-pencil" })
                        </td>
                            }
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
@Html.Hidden("id_xhrt", "K")

<div id="loader2" data-request-ajax2="@Url.Action("delete_list")"></div>
@section Scripts {
    <script>
    </script>
}
