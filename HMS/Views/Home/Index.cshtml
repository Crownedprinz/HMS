
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LogLayout.cshtml";
    HMS.Models.worksess worksess = (HMS.Models.worksess)Session["worksess"];
    string s1 = "0" + (DateTime.Now.Hour.ToString());
    string s2 = "0" + (DateTime.Now.Minute.ToString());
    string s3 = "0" + (DateTime.Now.Second.ToString());
    string secs = s2.Substring(s2.Length - 2, 2) + s3.Substring(s3.Length - 2, 2) + s1.Substring(s1.Length - 2, 2);
}

 <div class="row text-center " style="padding-top:100px;">
            <div class="col-md-12">
                @*<img src="~/img/nologo.png" />*@
                <img src="@Url.Action( "Show", "home", new { id = "COMPANY11" })" width="140" height="80" id="logo" />
            </div>

        </div>
         <div class="row ">
                
                <div class="col-md-4 col-md-offset-4 col-sm-6 col-sm-offset-3 col-xs-10 col-xs-offset-1">
                    <h4 style="color:red">@Session["err_Message"]</h4>
                            <div class="panel-body">
                                <form class="form-horizontal" role="form" method="post" enctype="multipart/form-data">
                                    @Html.AntiForgeryToken()
                                    @Html.ValidationSummary()
                                    <hr />
                                    <h5>Enter Details to Login</h5>
                                    <br />
                                    <div class="form-group input-group">
                                        <span class="input-group-addon"><i class="fa fa-tag"></i></span>
                                        @Html.TextBox("vwstring0", "", new { @maxlength = "10", placeholder = "Your Username ", @class = "form-control mypadding", data_val = "true", data_val_required = "User Id is required" })
                                   @Html.Hidden("vwstring2", @secs)
                                     </div>
                                    <div class="form-group input-group">
                                        <span class="input-group-addon"><i class="fa fa-lock"></i></span>
                                        @Html.Password("vwstring1", "", new { @class = "form-control mypadding ", placeholder = "Your Password", data_val = "true", data_val_required = "Password is required" })
                                    </div>
                                    <div class="form-group">
                                        <label class="checkbox-inline">
                                            <input type="checkbox" /> Remember me
                                        </label>
                                        @*<span class="pull-right">
                                            <a href="index.html">Forget password ? </a>
                                        </span>*@
                                    </div>
                                        <input type="submit" value="Login Now" class="btn btn-primary">
                                    &nbsp;&nbsp;&nbsp;
                                    <span class="text-centre"><a href="~/home/Cancel" class="btn btn-primary " style="width:100px">Cancel</a></span>

                                    <hr />
                                </form>
                            </div>
                           
                        </div>
                
           
        </div>

